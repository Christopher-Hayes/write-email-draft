{
  "name": "write-email-draft",
  "version": "0.2.0",
  "description": "Looks for unread emails in an IMAP inbox and drafts a reply using ai.",
  "main": "app.js",
  "scripts": {
    "start": "ts-node ./src/app.ts",
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only ./src/app.ts"
  },
  "license": "",
  "devDependencies": {
    "@types/node": "^20.12.7",
    "ts-node": "^10.9.2",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.4.5"
  },
  "compilerOptions": {
    "types": [
      "node"
    ]
  },
  "dependencies": {
    "@ai-sdk/openai": "^1.1.9",
    "ai": "^4.1.34",
    "dotenv": "^16.4.7",
    "imap": "^0.8.19",
    "mailparser": "^3.7.2"
  }
}
